name = "ats-scanner-free"
pages_build_output_dir = "dist"
compatibility_date = "2024-12-18"
main = "functions/api/analyze.js"

# Enable Cloudflare Pages Functions support
[env.production]
name = "ats-scanner-free"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "production"

# Note: GEMINI_API_KEY should be set via Cloudflare Dashboard → Pages → Settings → Environment Variables
# This provides secure secret management without exposing keys in config files
